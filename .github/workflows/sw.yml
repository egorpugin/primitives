name: C/C++ CI with SW

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - uses: egorpugin/sw-action
        
    - name: build
      run: ./sw build
